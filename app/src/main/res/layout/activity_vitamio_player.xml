<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:gravity="center"
    tools:context=".player.SystemPlayerActivity">

    <com.liushuang.liushuang_video.widget.VitamioVideoView
        android:id="@+id/id_vv_systemPlayer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerInParent="true"/>

    <include
        android:id="@+id/media_controller"
        layout="@layout/media_controller" />

    <include
        android:id="@+id/ll_buffer"
        layout="@layout/ll_buffer" />

    <include
        android:id="@+id/ll_loading"
        layout="@layout/ll_loading" />
</RelativeLayout>